onlyBuiltDependencies:
  - esbuild
  - msw
  # TODO(warlock-npm): remove this entry once @posthog/warlock is published to
  # npm. It's only needed because the git dependency runs a `prepare` build on
  # install; the published package ships prebuilt dist/ and needs no build.
  - '@posthog/warlock'
minimumReleaseAge: 10080
blockExoticSubdeps: true
trustPolicy: no-downgrade

packages:
  - '!e2e-tests/**'