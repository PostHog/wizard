/**
 * Revenue-analytics learn-deck. Currently delegates to the generic skill
 * deck; replace this re-export with a workflow-specific script when the
 * revenue narrative grows its own diagrams or talking points.
 */

export { getContentBlocks } from '../../agent-skill/content/index.js';
