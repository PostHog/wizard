/**
 * Feature flags + experiments learn material. Plays alongside the generic
 * migration deck when the source product is a feature-flag / experimentation
 * tool (e.g. Statsig). Placeholder content for now.
 *
 * Examples of what could go here: how PostHog represents flags vs configs vs
 * experiments, how flags wire into analytics and replays, how rollouts work,
 * how multivariate flags carry JSON payloads.
 */

import type { ContentBlock } from '../../../../ui/tui/primitives/content-types.js';

export const FEATURE_FLAGS_EXPERIMENTS_BLOCKS: ContentBlock[] = [
  { content: 'Feature flags and experiments in PostHog.', pause: 3000 },
  // TODO: replace with real FF/experiments narrative.
];
