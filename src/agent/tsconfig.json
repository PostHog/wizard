{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "../../.tsbuild/agent"
  },
  "include": ["**/*", "../../types/**/*.d.ts"],
  "references": [
    {
      "path": "../../tsconfig.env.json"
    },
    {
      "path": "../store"
    }
  ],
  "exclude": ["vitest.config.ts"]
}
