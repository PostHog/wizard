/**
 * Appears in exactly one built chunk. The smoke test proves published TUI
 * entry chunks never import that chunk and headless builds still carry it.
 */
export const CONTROL_SERVER_MARKER = 'wizard-control-server';
