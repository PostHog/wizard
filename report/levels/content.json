{"nav": "<nav><h1>Wizard surface split</h1><p class=\"small\">Goal to enforcement. Each level: layers, what crosses between them, the shape of each edge.</p><ol><li><a href=\"#l0\"><span class=\"toc-lvl\">L0</span>Goal</a></li><li><a href=\"#l1\"><span class=\"toc-lvl\">L1</span>Run surfaces</a></li><li><a href=\"#l2\"><span class=\"toc-lvl\">L2</span>Surfaces</a></li><li><a href=\"#l3\"><span class=\"toc-lvl\">L3</span>Objects</a></li><li><a href=\"#l4\"><span class=\"toc-lvl\">L4</span>Data</a></li><li><a href=\"#l5\"><span class=\"toc-lvl\">L5</span>Wire</a></li><li><a href=\"#l6\"><span class=\"toc-lvl\">L6</span>A run</a></li><li><a href=\"#l7\"><span class=\"toc-lvl\">L7</span>Enforcement</a></li></ol></nav>", "segments": [{"html": "<h1>The wizard, from the goal down to the checks</h1><p class=\"lead\">8 levels. Read top to bottom: each one zooms into the layers of the one above. Every diagram is a stack of layers; arrows run between layers and name what crosses and the shape it has.</p>"}, {"html": "<section id=\"l0\"><h2><span class=\"lvl\">L0</span> Goal: one codebase, three ways to run it</h2><p class=\"goal\">One process, four parts. The cli composes; the store holds state and every contract; the tui renders; the agent runs. A person, a headless workflow, or a parent on a socket can drive it, and the parts never know which.</p>"}, {"fig": "l0"}, {"html": "<table class=\"edges\"><thead><tr><th>Edge</th><th>Boundary crossed</th><th>Carries</th><th>Shape</th></tr></thead><tbody><tr><td>who drives it \u2192 cli</td><td>process entry</td><td>argv, the API key in env; [1] keys, [3] socket requests</td><td>yargs; TTY; HTTP JSON</td></tr><tr><td>cli \u2192 tui, agent</td><td>surface</td><td>start the render [1]; one run at a time</td><td><code>startTUI</code>; <code>runAgent(config, session)</code></td></tr><tr><td>cli \u2192 store</td><td>surface</td><td>the flow; one run store per run; hooks</td><td><code>FlowStore</code>, <code>startRun</code>; <code>ControlHooks</code></td></tr><tr><td>tui, agent \u2192 store</td><td>surface</td><td>state and commits; progress and outcome</td><td><code>FlowStoreApi</code>; <code>WizardUI</code></td></tr><tr><td>store, agent \u2192 outside</td><td>machine</td><td>auth, events; edits, model calls</td><td>HTTPS; files</td></tr></tbody></table><p class=\"note\">Way [2] is [1] without the tui. Way [3] is [2] with the store put on a socket. L1 shows each way step by step.</p></section>"}, {"html": "<section id=\"l1\"><h2><span class=\"lvl\">L1</span> Run surfaces: what each way in actually does</h2><p class=\"goal\">The TUI is unchanged. The other four surfaces are the same non-interactive pipeline or the same TUI with a control server attached. A control server never runs anything on its own: it mirrors the store and waits for a parent.</p>"}, {"fig": "l1"}, {"html": "<table class=\"edges\"><thead><tr><th>Surface</th><th>Command \u00b7 build</th><th>What runs</th><th>How it ends</th></tr></thead><tbody><tr><td>[1] TUI</td><td><code>npx @posthog/wizard [command]</code> \u00b7 published</td><td>Ink render with <code>StoreUI</code>; OAuth in the browser; gates <code>intro</code>, <code>auth</code>; the flow's steps; one <code>startRun</code> + <code>runAgent</code> per run step</td><td>outro, then mcp, slack-connect, keep-skills; exit 1 if the run failed</td></tr><tr><td>[2] headless</td><td><code>--headless-DONOTUSE-EXPERIMENTAL --api-key --project-id --install-dir [--region]</code> \u00b7 published, hidden flag</td><td><code>LoggingUI</code> + <code>HeadlessUI</code> (console output, tasks into the store); API key \u2192 credentials, no browser; <code>ciPreRun</code> or the <code>onReady</code> walk; one <code>runAgent</code>; the gateway token is minted</td><td>outro to the log; task stream pushed to PostHog unless <code>--no-telemetry</code>; <code>--task-stream-log</code> also dumps; exit 0, or 1 through <code>wizardAbort</code></td></tr><tr><td>[2b] CI</td><td><code>--ci --api-key --project-id --install-dir</code> \u00b7 dev builds only</td><td>the same pipeline with build tag <code>ci</code>; gateway bearer from <code>WIZARD_CI_GATEWAY_TOKEN_FILE</code> instead of a mint; asks disabled unless <code>--e2e-ask</code></td><td>task stream dumped to a file (default path), never pushed; exit 0/1</td></tr><tr><td>[3] controlled headless</td><td><code>--headless\u2026 --control-socket &lt;path&gt;</code> \u00b7 published</td><td><code>StoreUI</code>, <code>e2eAsk</code> on; the control server serves the store and waits; a parent detects, sets the store, and starts runs; each <code>POST /runs</code> is one agent on a fresh <code>RunStore</code>; <code>/run</code> and TUI actions answer 501</td><td><code>POST /shutdown</code> ends the process with 0; the ledger keeps every run's end state</td></tr><tr><td>[3b] controlled TUI</td><td><code>--ci --control-socket &lt;path&gt; [--e2e-ask]</code> \u00b7 dev builds only</td><td>the real TUI with a <code>ci</code> session (API key, no browser) and the control server attached; the parent commits what a keyboard would (<code>/actions</code>), resolves credentials (<code>/credentials</code>), and releases the flow's run (<code>/run</code>); <code>/detect</code> and <code>/runs</code> answer 501</td><td>the flow ends as in [1]; the parent reads frames through a PTY</td></tr><tr><td>individual agents</td><td>controlled headless, or in process</td><td><code>POST /runs { programId }</code>, <code>{ skillId }</code> (generic <code>agent-skill</code>), plus <code>installDir</code>, <code>frameworkContext</code>, <code>config</code> overlay; in process <code>runAgent(runConfigFor(config), session)</code></td><td>one run, one <code>RunRecord</code>; 409 while one is in flight</td></tr></tbody></table><p class=\"note\">Published builds refuse <code>--control-socket</code> without the headless flag and never carry the control server in the TUI chunk; the smoke test audits both.</p>\n<h3>Recipes</h3>\n<p><b>A whole program, headless, driven over the socket.</b> The launched program only names the flow the store registers; every run is chosen per request.</p>\n<pre class=\"code\">S=/tmp/w/w.sock   # a directory you own, mode 0700\nPOSTHOG_WIZARD_API_KEY=&lt;personal key&gt; node dist/bin.js --headless-DONOTUSE-EXPERIMENTAL \\\n  --control-socket $S --project-id 228144 --region us --install-dir /path/to/app &amp;\ncurl -s --unix-socket $S http://x/health\ncurl -s --unix-socket $S -X POST http://x/detect -d '{}'                       # ciPreRun or onReady: framework, credentials\ncurl -s --unix-socket $S -X POST http://x/runs -d '{\"programId\":\"posthog-integration\"}'\ncurl -s --unix-socket $S 'http://x/state?wait=120000&amp;since=17' | jq .state.session.runPhase   # long poll until it settles\ncurl -s --unix-socket $S http://x/runs | jq '.runs[].status'                    # wait for \"done\" before shutting down\ncurl -s --unix-socket $S -X POST http://x/shutdown -d '{}'                     # process exits 0, socket unlinked</pre>\n<p><b>Individual agents.</b> Each <code>POST /runs</code> is one independent run on a fresh RunStore; a second one while the first is in flight is a 409.</p>\n<pre class=\"code\">curl -s --unix-socket $S -X POST http://x/runs -d '{\"skillId\":\"audit-events\"}'                 # any catalog skill, on the generic agent-skill program\ncurl -s --unix-socket $S -X POST http://x/runs -d '{\"programId\":\"metrics\",\"installDir\":\"apps/web\",\"frameworkContext\":{\"router\":\"app\"}}'\ncurl -s --unix-socket $S -X POST http://x/runs -d '{\"programId\":\"audit\",\"config\":{\"allowedTools\":[\"Read\",\"Grep\"],\"requiresAi\":false,\"streamWorkflowId\":\"audit-nightly\"}}'</pre>\n<p><b>Set the store first, then run.</b> Setters are screen independent; <code>GET /store</code> lists the 25 the wire accepts.</p>\n<pre class=\"code\">curl -s --unix-socket $S http://x/store | jq '.setters[].name'\ncurl -s --unix-socket $S -X POST http://x/store/setFrameworkConfig  -d '{\"params\":{\"integration\":\"nextjs\"}}'\ncurl -s --unix-socket $S -X POST http://x/store/setFrameworkContext -d '{\"params\":{\"key\":\"router\",\"value\":\"app\"}}'\ncurl -s --unix-socket $S -X POST http://x/store/setCredentials      -d '{\"params\":{\"accessToken\":\"\u2026\",\"projectApiKey\":\"\u2026\",\"projectId\":228144,\"apiHost\":\"https://us.posthog.com\"}}'</pre>\n<p><b>The real TUI under a parent (dev builds).</b> Same store, real Ink; the parent commits what a keyboard would and reads the frame back through a PTY. This is what the snapshot route and the MCP proxy do.</p>\n<pre class=\"code\">POSTHOG_WIZARD_API_KEY=&lt;key&gt; npx tsx bin.ts --ci --control-socket $S --project-id 228144 --install-dir /path/to/app --e2e-ask\ncurl -s --unix-socket $S http://x/state | jq '.state.currentScreen, [.state.actions[].id]'\ncurl -s --unix-socket $S -X POST http://x/actions/confirm_setup -d '{}'\ncurl -s --unix-socket $S -X POST http://x/credentials -d '{}'    # API key \u2192 project credentials, advances auth\ncurl -s --unix-socket $S -X POST http://x/run -d '{}'            # releases the flow's own agent start\ncurl -s --unix-socket $S -X POST http://x/actions/answer_question -d '{\"params\":{\"answers\":{\"q1\":\"yes\"}}}'</pre>\n<p><b>In process.</b> <code>runAgent(runConfigFor(config), session, { composed })</code> from <code>@agent</code> is one run with no flow and no screens; <code>cli/main.ts</code> has already installed a <code>LoggingUI</code>.</p>\n</section>"}, {"html": "<section id=\"l2\"><h2><span class=\"lvl\">L2</span> Surfaces: the import boundary</h2><p class=\"goal\">Four source roots. A surface imports another only through its public entries; instances cross the other way by injection. store imports no surface; tui and agent never meet.</p>"}, {"fig": "l2"}, {"html": "<table class=\"edges\"><thead><tr><th>Edge</th><th>Boundary crossed</th><th>Carries</th><th>Shape</th></tr></thead><tbody><tr><td>cli \u2192 tui, agent</td><td>surface</td><td>the render entry, console renderers, one run</td><td><code>startTUI \u2192 TuiHandle</code>; <code>LoggingUI</code>; <code>runAgent(config, session, { composed })</code></td></tr><tr><td>cli \u2192 store</td><td>surface</td><td>the store class, the UI accessor, programs, the control server</td><td><code>@store</code>, <code>@store/types</code>, <code>@store/programs</code>; <code>@store/control</code> dynamic</td></tr><tr><td>tui \u2192 store</td><td>surface</td><td>what screens read and commit</td><td><code>FlowStoreApi = Pick&lt;FlowStore, 63 names&gt;</code>; <code>Flow</code></td></tr><tr><td>agent \u2192 store</td><td>surface</td><td>how a run reports and what it reads</td><td><code>WizardUI</code> (44 methods); <code>ProgramRunConfig</code>; tool behaviour</td></tr><tr><td>store \u2192 any surface</td><td>surface</td><td>nothing</td><td>instances arrive by <code>setUI</code>, <code>new FlowStore(flow)</code>, hooks</td></tr></tbody></table><p class=\"note\">Enforced three ways (L7): the compiler, an architecture test suite, a bundle audit.</p></section>"}, {"html": "<section id=\"l3\"><h2><span class=\"lvl\">L3</span> Objects: what attaches to what at runtime</h2><p class=\"goal\">One FlowStore per process, one RunStore per agent run. Every writer reaches the store through one contract; every reader subscribes to one store. The agent sees only WizardUI and its own session copy.</p>"}, {"fig": "l3"}, {"html": "<table class=\"edges\"><thead><tr><th>Edge</th><th>Boundary crossed</th><th>Carries</th><th>Shape</th></tr></thead><tbody><tr><td>runner \u2192 FlowStore</td><td>cli \u2192 store</td><td>the flow to register</td><td><code>new FlowStore(flow)</code>; <code>startTUI</code> does it for the TUI</td></tr><tr><td>FlowStore \u2192 RunStore</td><td>inside store</td><td>the session a run inherits</td><td><code>startRun(session): RunStore</code>; run keys reset by <code>RUN_SESSION_DEFAULTS</code></td></tr><tr><td>runner \u2192 agent</td><td>cli \u2192 agent</td><td>the run config and the run's session copy</td><td><code>runAgent(runConfigFor(config), run.session, { composed })</code></td></tr><tr><td>agent \u2192 StoreUI \u2192 FlowStore</td><td>agent \u2192 store</td><td>status, tasks, tokens, asks, notices, outro</td><td><code>WizardUI</code> \u2192 one <code>FlowStoreApi</code> setter each</td></tr><tr><td>UiStore \u2192 FlowStore</td><td>tui \u2192 store</td><td>change notifications</td><td><code>subscribe</code>; derives <code>activeScreen</code></td></tr><tr><td>TaskStreamPush \u2192 RunStore</td><td>inside store</td><td>the run's tasks, plan, handoff</td><td><code>RunStateSource</code> (6 members)</td></tr><tr><td>control server \u2192 FlowStore</td><td>inside store</td><td>the projection out, setters in</td><td><code>readState(): ControlState</code>; <code>CONTROL_SETTERS</code></td></tr></tbody></table></section>"}, {"html": "<section id=\"l4\"><h2><span class=\"lvl\">L4</span> Data: who owns which key</h2><p class=\"goal\">A key lives in exactly one place. The flow keeps what a later run must see; a run keeps what a later run must not. The wire gets a whitelist of the merge and never a secret.</p>"}, {"fig": "l4"}, {"html": "<table class=\"edges\"><thead><tr><th>Edge</th><th>Boundary crossed</th><th>Carries</th><th>Shape</th></tr></thead><tbody><tr><td>flow \u2192 store.session</td><td>inside store</td><td>everything that outlives a run</td><td><code>WizardSession</code> map</td></tr><tr><td>run \u2192 store.session</td><td>inside store</td><td>the active run's keys</td><td><code>RUN_SESSION_KEYS</code> (8) laid over the flow</td></tr><tr><td>store.session \u2192 wire</td><td>process \u2192 socket</td><td>the reader's view</td><td><code>ControlSession = Pick&lt;WizardSession, 21 keys&gt; &amp; { hasCredentials, projectId }</code></td></tr><tr><td>never on the wire</td><td>process \u2192 socket</td><td>token, key, user record, answers, vault</td><td>answers recorded as ids and prompts only</td></tr></tbody></table><p class=\"note\">The two artefact URLs moved to the flow after a real self-driving run showed run 2 wiping what run 1 created.</p></section>"}, {"html": "<section id=\"l5\"><h2><span class=\"lvl\">L5</span> Wire: the control API as the store on a socket</h2><p class=\"goal\">A route is a store question, a store setter, or one of four hooks for what the store cannot do. Screen-gated actions keep keyboard parity; named setters give a parent full store controls; a run request lays data fields over a program.</p>"}, {"fig": "l5"}, {"html": "<table class=\"edges\"><thead><tr><th>Edge</th><th>Boundary crossed</th><th>Carries</th><th>Shape</th></tr></thead><tbody><tr><td>GET /state</td><td>socket</td><td>the store, read</td><td><code>ControlState</code>; <code>?wait=&amp;since=</code> resolves on <code>version &gt; since</code></td></tr><tr><td>POST /actions/:id</td><td>socket</td><td>a commit legal on the current screen</td><td><code>{ params }</code> \u2192 the setter the key handler calls</td></tr><tr><td>POST /store/:setter</td><td>socket</td><td>a commit whatever the screen</td><td><code>{ params }</code> \u2192 one of 25 <code>CONTROL_SETTERS</code>, each a boundary member</td></tr><tr><td>POST /runs</td><td>socket \u2192 hook</td><td>one independent run</td><td><code>programId</code> or <code>skillId</code>, <code>installDir?</code>, <code>frameworkContext?</code>, <code>config?</code> (7 data fields)</td></tr><tr><td>GET /runs</td><td>socket</td><td>the ledger</td><td><code>RunRecord</code>: runId, programId, skillId, installDir, status, error, timestamps, result</td></tr><tr><td>POST /credentials \u00b7 /detect \u00b7 /shutdown</td><td>socket \u2192 hook</td><td>what the store cannot do</td><td><code>ControlHooks</code>; 409 while running, 501 on the other surface</td></tr><tr><td>errors</td><td>socket</td><td>every bad input</td><td>400 \u00b7 404 \u00b7 409 \u00b7 413 (&gt; 64 KB) \u00b7 415 \u00b7 500 \u00b7 501</td></tr></tbody></table></section>"}, {"html": "<section id=\"l6\"><h2><span class=\"lvl\">L6</span> A run: what goes in, what comes out</h2><p class=\"goal\">Independent by construction. The agent receives a run config and a session copy, speaks only through WizardUI, and its RunStore is discarded by the next run. Composition lives above, in the cli or the parent.</p>"}, {"fig": "l6"}, {"html": "<table class=\"edges\"><thead><tr><th>Edge</th><th>Boundary crossed</th><th>Carries</th><th>Shape</th></tr></thead><tbody><tr><td>config \u2192 agent</td><td>store \u2192 agent</td><td>what to run</td><td><code>ProgramRunConfig</code>; functions never cross the socket, data fields may be overlaid</td></tr><tr><td>session \u2192 agent</td><td>store \u2192 agent</td><td>as whom and where</td><td><code>run.session</code>, a copy</td></tr><tr><td>agent \u2192 RunStore</td><td>agent \u2192 store</td><td>progress and outcome</td><td><code>WizardUI</code> \u2192 <code>StoreUI</code> \u2192 setters</td></tr><tr><td>gates</td><td>agent \u2194 store</td><td>when to wait</td><td><code>waitForAiOptIn</code>, <code>waitForGate(id)</code>; unknown ids resolve at once</td></tr><tr><td>outcome \u2192 parent</td><td>store \u2192 socket</td><td>how it ended</td><td><code>RunRecord.result</code>; <code>runPhase</code> settles before the record does</td></tr></tbody></table></section>"}, {"html": "<section id=\"l7\"><h2><span class=\"lvl\">L7</span> Enforcement: what fails if a boundary moves</h2><p class=\"goal\">The same rule is checked three ways so it cannot drift in one place. Under them, one Vitest project per surface and real socket-driven runs.</p>"}, {"fig": "l7"}, {"html": "<table class=\"edges\"><thead><tr><th>Edge</th><th>Boundary crossed</th><th>Carries</th><th>Shape</th></tr></thead><tbody><tr><td>compiler</td><td>every import</td><td>a surface that references what it may not</td><td><code>tsc -b</code> fails</td></tr><tr><td>architecture suite</td><td>every source file</td><td>the matrix, public entries, consumed names, the store boundary</td><td>6 specs; goldens</td></tr><tr><td>bundle audit</td><td>the built chunks</td><td>the control chunk and the TUI chunk</td><td>markers; fixtures compared in CI</td></tr><tr><td>per-surface projects</td><td>tests</td><td>a surface's own behaviour</td><td>aliases restricted; <code>ink</code> throws outside tui and harness</td></tr><tr><td>real runs</td><td>the whole process</td><td>what a user sees</td><td>screen paths and frames against the previous phase</td></tr></tbody></table></section>"}, {"html": "<p class=\"small\">Deep dive with runtime scenarios, evidence, review findings, and decisions: the canvas \"Wizard surface split: architecture report\" in this channel. Source PRs: PostHog/wizard #1269 to #1283.</p>"}], "figures": ["l0", "l1", "l2", "l3", "l4", "l5", "l6", "l7"], "generated": "2026-09-21"}